function unassignEmployeeProject(emp_id, project_id) {
    // Here you can implement the logic for assigning a project to an employee
    console.log('Assigning project ' + project_id + ' to employee ' + emp_id);
}

function unassignManagerProject(manager_id, project_id) {
    // Here you can implement the logic for assigning a project to a manager
    console.log('Assigning project ' + project_id + ' to manager ' + manager_id);
}